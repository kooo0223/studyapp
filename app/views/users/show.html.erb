<div class="wrapper">
  <div class="side-bar">
    <%= render "posts/side_bar" %>
  </div>
  <div class="main">
    <p><%= @nickname %>さんの投稿一覧</p>
    <% @posts.each do |post| %>
      <%= render partial: "posts/post", locals: { post: post } %>
    <% end %>
  </div>
</div>