<div class="contents row">
  <p><%= @nickname %>さんの投稿一覧</p>
  <% @posts.each do |post| %>
    <div class="content_post" style="background-image: url(<%= post.question_image %>);">
      <p><%= post.question_text %></p>
      <p><%= post.answer_text %></p>
    </div>
  <% end %>
</div>